name: database_provider_native
description: "Database provider native implementation"
version: 0.0.1

publish_to: 'none'

environment:
  sdk: '>=3.12.0 <4.0.0'
  flutter: '3.44.6'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  logging: ^1.3.0
  path_provider: ^2.1.6
  path: ^1.9.1

  drift: ^2.34.3
  sqlite3: ^3.5.1

  database_utils:
    path: "../database_utils"
