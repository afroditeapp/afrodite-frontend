name: database_common
version: 1.0.0

publish_to: none

environment:
  sdk: '>=3.13.0 <4.0.0'

resolution: workspace

dependencies:
  drift: ^2.34.3

  openapi:
    path: "../api_client"

  database_model:
    path: "../database_model"
  database_converter:
    path: "../database_converter"
  database_utils:
    path: "../database_utils"

  utils:
    path: "../utils"

dev_dependencies:
  build_runner: ^2.15.1
  drift_dev: ^2.34.0
