name: database_provider_web
description: "Database provider web implementation"
version: 0.0.1

publish_to: 'none'

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: '>=3.3.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  logging: ^1.2.0
  drift: ^2.26.0

  database:
    path: "../database"

  utils:
    path: "../utils"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
