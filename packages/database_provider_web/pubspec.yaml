name: database_provider_web
description: "Database provider web implementation"
version: 0.0.1

publish_to: 'none'

environment:
  sdk: '>=3.3.2 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter

  drift: ^2.19.1+1

  database:
    path: "../database"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
