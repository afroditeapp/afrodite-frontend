name: database_provider_web
description: "Database provider web implementation"
version: 0.0.1

publish_to: 'none'

environment:
  sdk: '>=3.9.0 <4.0.0'

resolution: workspace

dependencies:
  logging: ^1.2.0
  drift: ^2.26.0

  database_utils:
    path: "../database_utils"

  utils:
    path: "../utils"
