name: database_provider_web
description: "Database provider web implementation"
version: 0.0.1

publish_to: 'none'

environment:
  sdk: '>=3.6.0 <4.0.0'

resolution: workspace

dependencies:
  logging: ^1.2.0
  drift: ^2.26.0

  database:
    path: "../database"

  utils:
    path: "../utils"

dev_dependencies:
  lints: ^6.0.0
