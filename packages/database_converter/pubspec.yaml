name: database_converter
version: 1.0.0

publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'

resolution: workspace

dependencies:
  drift: ^2.26.0

  openapi:
    path: "../api_client"

  database_model:
    path: "../database_model"

  utils:
    path: "../utils"
