targets:
  $default:
    builders:
      drift_dev:
        generate_for:
          include:
            - lib/database/**.dart
      freezed:
        options:
          map: false
          when:
            when: false
            maybe_when: false
            when_or_null: false
        generate_for:
          include:
            - lib/model/freezed/**.dart
